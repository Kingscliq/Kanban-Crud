@model List<Tasks.Models.TodosModel>

@{
    ViewData["Title"] = "Contact Page";
}

<partial name="_Notifications" />
<div>
    <h1 class="fs-2 fw-bold">Todo lists for Today</h1>
    <hr/>
    <ul class="list-group">
        @foreach (var task in Model)
        {
            <li class="list-group-item">
                <div class="d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center">
                    <div>
                        <form>
                            <input onchange="" class="checkbox" id="isCompleted" style="height: 30px; width: 30px" type="checkbox" checked=@task.isCompleted>
                        </form>
                        
                    </div>
                    <div class="mx-3">
                    <label class="form-check-label fs-6" id="title"> @task.title</label>
                    <div>
                        <small class="text-secondary" id="description">@task.description</small>
                    </div>
                    </div>
                </div>
                    <div class="btn-group">
                        <a asp-controller="Todos" asp-action="Edit" asp-route-id=@task.Id class="text-secondary btn"><i class="bi bi-pencil-square"></i>Edit</a>
                        <a asp-controller="Todos" asp-action="Delete" asp-route-id=@task.Id class="ml-2 text-danger btn"><i class="bi bi-trash3"></i>Delete</a>
                    </div>
                </div>
                  
            </li>
        }
    </ul>
</div>

<script type="text/javascript">
    const checkBox = document.querySelector("#isCompleted");
    const title = document.querySelector("#title");
    const description = document.querySelector("#description");

    if (checkBox.checked)

</script>


